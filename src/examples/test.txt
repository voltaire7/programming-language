# (define (fib n)
  # (if (< n 2)
      # n
      # (+ (fib (- n 1)) (fib (- n 2)))))

# ;; Example usage
# (fib 10)  ;; => 55

let fib [
    let [n] args 1
    if < n 2 [
        n
    ] [
        + fib - n 1 fib - n 2
    ]
]
print\n fib 10  # outputs 55


